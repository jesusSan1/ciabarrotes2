/*!
 * Start Bootstrap - SB Admin 2 v4.1.4 (https://startbootstrap.com/theme/sb-admin-2)
 * Copyright 2013-2021 Start Bootstrap
 * Licensed under MIT (https://github.com/StartBootstrap/startbootstrap-sb-admin-2/blob/master/LICENSE)
 */

!(function (l) {
  "use strict";
  l("#sidebarToggle, #sidebarToggleTop").on("click", function (e) {
    l("body").toggleClass("sidebar-toggled"),
      l(".sidebar").toggleClass("toggled"),
      l(".sidebar").hasClass("toggled") &&
        l(".sidebar .collapse").collapse("hide");
  }),
    l(window).resize(function () {
      l(window).width() < 768 && l(".sidebar .collapse").collapse("hide"),
        l(window).width() < 480 &&
          !l(".sidebar").hasClass("toggled") &&
          (l("body").addClass("sidebar-toggled"),
          l(".sidebar").addClass("toggled"),
          l(".sidebar .collapse").collapse("hide"));
    }),
    l("body.fixed-nav .sidebar").on(
      "mousewheel DOMMouseScroll wheel",
      function (e) {
        var o;
        768 < l(window).width() &&
          ((o = (o = e.originalEvent).wheelDelta || -o.detail),
          (this.scrollTop += 30 * (o < 0 ? 1 : -1)),
          e.preventDefault());
      }
    ),
    l(document).on("scroll", function () {
      100 < l(this).scrollTop()
        ? l(".scroll-to-top").fadeIn()
        : l(".scroll-to-top").fadeOut();
    }),
    l(document).on("click", "a.scroll-to-top", function (e) {
      var o = l(this);
      l("html, body")
        .stop()
        .animate(
          { scrollTop: l(o.attr("href")).offset().top },
          1e3,
          "easeInOutExpo"
        ),
        e.preventDefault();
    });
})(jQuery);

// Para poner el nombre del modulo
let contenido = window.location.pathname.split("/")[1];
let titulo = document.querySelector("#titulo");
switch (contenido) {
  case "dashboard":
    titulo.innerHTML = "Menú principal";
    break;
}

// Libreria zoomerang
Zoomerang.config({
  maxHeight: 400,
  maxWidth: 400,
  bgColor: "#000",
  bgOpacity: 0.85,
  onOpen: openCallback,
  onClose: closeCallback,
  onBeforeOpen: beforeOpenCallback,
  onBeforeClose: beforeCloseCallback,
}).listen(".imagen");

function openCallback(el) {}

function closeCallback(el) {}

function beforeOpenCallback(el) {}

function beforeCloseCallback(el) {}

//* Controlar los menus con el hash
document.addEventListener("DOMContentLoaded", () => {
  if (window.location.hash) {
    const hash = window.location.hash.substring(1);
    switch (hash) {
      case "lista-categorias":
        borrarHash(".lista-categorias");
        break;
      case "lista-empleados":
        borrarHash(".lista-empleados");
        break;
      case "lista-proveedores":
        borrarHash(".lista-proveedores");
        break;
      case "producto-almacen":
        borrarHash(".producto-almacen");
        break;
      case "producto-minimo":
        borrarHash(".producto-minimo");
        break;
      case "producto-caducado":
        borrarHash(".producto-caducado");
        break;
    }
  }
});

function borrarHash(el) {
  const menu = document.querySelector(el);
  menu.click();
  window.history.pushState("", document.title, window.location.pathname);
}
